# 最终覆盖审计

对以下范围执行最终产品覆盖审计：

- `sop/PRD.md`
- `sop/TUC.md`（若存在）
- `sop/NTC.md`（若存在）
- `sop/ECS.md`（若存在）
- `sop/TODO.md`（若存在）
- `prisma/schema.prisma`
- `prisma/seed.ts`
- `backend/`
- `docs/api.md`
- `web/`
- `web-e2e/workflows.md`
- `web-e2e/`

检查：

- 有 PRD 功能但无实现
- TUC / NTC / ECS 用例缺少后端或 E2E 覆盖
- 本应已关闭但仍开放的 `sop/TODO.md` 条目
- 有已实现领域功能但文档缺失
- 有 UI 控件但无真实行为
- 死按钮
- 死菜单
- 死路由
- 过时 mock 数据
- 过时本地持久化
- 有 API 端点但无后端测试
- 重要后端功能无 E2E 路径
- 有工作流步骤但无 Playwright 覆盖
- 有交互控件但无覆盖
- 过时 API 文档
- 本应由后端处理却从未处理的 schema 字段
- 与后端行为不一致的前端假设

修复真实缺口。

若 API 有意可在当前 Web 应用之外复用，不要强迫每个后端 API 能力都有 GUI。

最后运行：

- 后端测试
- web 构建/类型检查
- Playwright E2E 测试

所有必需测试必须通过。
