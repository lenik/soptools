Parse: multi-parts
Interaction: select
Discard: -*- mode: markdown -*-
Discard: vim: set ft=markdown :

# 编写后端实现的 Codex 提示词

为该 WorldMan 模块编写完整的 Codex 提示词以创建后端。

后端技术栈：

- `pnpm`
- Node.js
- TypeScript
- Fastify
- Prisma

API 前缀：`/api/v1/`

端口 / 部署期望（写入提示词）：

- 基准 `N` 来自 `wm port -n <name>`（或 `worldman.json` 的 `portBase`）
- 直接运行 backend HTTP/HTTPS：`2000+N` / `2001+N`
- Apache/发布映射 HTTP/HTTPS：`6000+N` / `6001+N`
- 容器 bridge 内部 HTTP/HTTPS：`3000` / `3001`
- 规则：HTTPS = HTTP + 1；例外为 bridge web 的 `80`/`443`

身份 / 集成约束：

- 主数据主键使用 `cuid(2)`
- 对等模块实体为不透明 cuid2 引用 + API 解析，不是跨库外键
- 在对等模块缺失时，本模块仍须能以最小本地基础数据表独立运行

后端 API 必须覆盖领域的有用能力，而不仅是当前 Web 已用到的功能。

分析每个领域对象的性质，并暴露可被以下方充分复用的能力：

- 当前 Web 应用
- 未来移动应用
- 其他内部系统
- 外部集成
- 自动化
- 报表
- 在合适时的 AI agent

对每个资源，评估是否需要：

- CRUD
- 列表
- 详情
- 搜索
- 查找
- 自动补全
- 过滤
- 排序
- 分页
- 批量获取
- 批量创建/更新/删除
- 关系遍历
- 树遍历
- 状态转换
- 历史
- 版本访问
- 校验
- 统计
- 聚合
- 导入
- 导出
- 媒体操作
- 外部引用解析

不要机械地给每个模型实现全部能力。

仅在对领域有意义时实现。

后端架构应分离：

- 路由/控制器层
- 校验
- 服务/领域逻辑
- 数据访问
- 共享错误
- API 序列化

避免把领域逻辑直接放在路由处理函数中。

包含健壮校验与一致的 API 错误。

提示词应要求 Codex 基于现有 Prisma schema 实现后端。

除非发现阻塞实现的矛盾，否则不要要求 Codex 重新设计产品。
