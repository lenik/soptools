# 添加 Playwright 路由冒烟测试

在 `web-e2e/` 下添加 Playwright E2E 测试。

先实现路由冒烟覆盖。

枚举应用的路由表。

对每个可到达的应用路由：

- 打开该路由
- 验证页面渲染
- 验证不崩溃
- 验证无致命页面错误
- 验证无意外未捕获异常
- 验证页面非空白

动态路由应使用有效的 seed 数据 ID，或创建所需测试数据。

不要把路由冒烟测试当作足够的功能覆盖。
