Save-As: sop/PRD.md
File-Link: inode
Parse: multi-parts
Discard: -*- mode: markdown -*-
Discard: vim: set ft=markdown :

# 编写 PRD

## 先介绍 WorldMan 架构

编写 PRD 之前，先说明并落实本模块的 WorldMan 架构：

- WorldMan 应用类似轻量微服务：每个应用既可**完全独立运行**，也可与对等应用**集成**。
- **独立运行**：本应用可拥有基础数据的**简化本地表**（例如联系人仅姓名/电话）。专用对等应用（如联系人应用）可有更完整模型。
- **集成运行**：对他应用主数据只保存 **cuid2 不透明引用**，通过对方 API 解析；不默认跨库外键。
- 主数据主键统一使用 **`cuid(2)`**，以便独立与集成部署下 id 稳定可迁移。
- 标准布局：`prisma/`、`backend/`、`web/`、`web-e2e/`、可选 `mobile/` / `mobile-e2e/`、`sop/`、`docs/`、`docker/`、`i-local/`、`i-medium/`、`worldman.json`。
- 部署：直接进程、Apache/Nginx 反代、或容器。基准端口 `N` 来自 `wm port -n <name>`（HTTPS = HTTP + 1；bridge 下 web 的 `80`/`443` 除外，详见 SOP README）。

PRD 须在适用处体现上述约束。

## 产出 PRD

根据此前所有讨论和当前产品设计整理完整 `sop/PRD.md`。

要求：

- 用于开发和后续验收，不只是领导汇报材料。
- 以当前已经讨论确认的需求为准。
- 不擅自扩大产品边界。
- 对存在明确业务含义的内容进行补全和规范化。
- 对前面讨论中隐含但已经可以推导出的约束进行明确描述。

PRD 至少覆盖（使用中文目录，勿依赖英文 TOC）：

- 产品概述
- 目标
- 范围
- 范围外
- 术语
- 用户角色
- 领域概念
- 核心实体
- 实体关系
- 业务规则
- 生命周期/状态转换
- CRUD 行为
- 搜索
- 过滤
- 排序
- 分页（如相关）
- 批量操作
- 导入/导出（如相关）
- 媒体
- 历史/审计（如相关）
- 外部引用
- 校验规则
- 删除/归档行为
- 空状态
- 错误行为
- 重要边界情况
- 主要 UI 工作流
- 非功能期望
- WorldMan 独立运行与集成模型

WorldMan 模块约束（必须写入 PRD）：

- 本模块的独立 vs 集成运行方式。
- 哪些实体是本模块拥有的主数据，哪些是对等模块的不透明 cuid2 引用。
- 主数据主键使用 `cuid(2)`。
- 相关时的部署/端口期望。

对于跨应用引用：

- 明确说明逻辑引用对象与所属模块。
- 不自动假定数据库外键。
- 使用 `<xxx>_id` 等稳定 cuid2 标识时，说明本模块拥有还是仅引用。

当讨论产出可测试材料时，亦在 `sop/` 下初始化或更新：

- `TODO.md` — 剩余 PRD / 实现任务；保持状态最新
- `TUC.md` — 从讨论中提取或构造的测试用例参考集
- `NTC.md` — 反用例 / 负面测试用例
- `ECS.md` — 边界场景

这些文件供后续 e2e 工作流与工具类/支撑类工作使用。

提供 `sop/PRD.md` 的下载链接。
